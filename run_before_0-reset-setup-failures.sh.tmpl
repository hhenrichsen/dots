{{- if or (eq .chezmoi.os "linux") (eq .chezmoi.os "darwin") -}}
#!/bin/sh
set -u

failures_log="${XDG_STATE_HOME:-$HOME/.local/state}/chezmoi/setup-failures.log"
{ mkdir -p "${failures_log%/*}" && : > "$failures_log"; } \
    || echo "WARN: cannot reset the setup failures log" >&2
exit 0
{{- end }}
